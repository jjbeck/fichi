import { ASTVisitor } from 'graphql-upload/node_modules/graphql/language/visitor';
import { SDLValidationContext } from 'graphql-upload/node_modules/graphql/validation/ValidationContext';

/**
 * Unique operation types
 *
 * A GraphQL document is only valid if it has only one type per operation.
 */
export function UniqueOperationTypesRule(
  context: SDLValidationContext,
): ASTVisitor;
