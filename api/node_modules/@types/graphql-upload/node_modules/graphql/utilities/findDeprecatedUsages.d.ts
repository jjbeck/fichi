import { GraphQLError } from 'graphql-upload/node_modules/graphql/error/GraphQLError';
import { DocumentNode } from 'graphql-upload/node_modules/graphql/language/ast';
import { GraphQLSchema } from 'graphql-upload/node_modules/graphql/type/schema';

/**
 * A validation rule which reports deprecated usages.
 *
 * Returns a list of GraphQLError instances describing each deprecated use.
 *
 * @deprecated Please use `validate` with `NoDeprecatedCustomRule` instead:
 *
 * ```
 * import { validate, NoDeprecatedCustomRule } from 'graphql'
 *
 * const errors = validate(schema, document, [NoDeprecatedCustomRule])
 * ```
 */
export function findDeprecatedUsages(
  schema: GraphQLSchema,
  ast: DocumentNode,
): ReadonlyArray<GraphQLError>;
